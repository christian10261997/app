// Temporary stubbed Firebase config to remove Firebase usage
// Exports minimal placeholders so imports in hooks/components do not break

// Types are intentionally broad to avoid type errors without Firebase
export const auth: any = undefined;
export const db: any = undefined;
export const storage: any = undefined;

const app: any = undefined;
export default app;
