rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only read/write their own user document
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Example: If you have other collections, add specific rules
    // For example, if you have a recipes collection:
    // match /recipes/{recipeId} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && 
    //     request.auth.uid == resource.data.authorId;
    // }
    
    // Example: If you have shared data that all users can read:
    // match /public/{document} {
    //   allow read: if request.auth != null;
    //   allow write: if false; // Only admins through server-side
    // }
    
    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
